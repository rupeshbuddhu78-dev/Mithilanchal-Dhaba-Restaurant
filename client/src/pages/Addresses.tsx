import { useAuth } from "@/_core/hooks/useAuth";
import { StorefrontLayout } from "@/components/StorefrontLayout";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";
import { MapPin, Pencil, Trash2 } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";

type AddressForm = { id?: number; label: string; recipientName: string; phone: string; line1: string; line2: string; city: string; state: string; pincode: string; deliveryInstructions: string; isDefault: boolean };
const blank: AddressForm = { label: "Home", recipientName: "", phone: "", line1: "", line2: "", city: "", state: "West Bengal", pincode: "", deliveryInstructions: "", isDefault: false };
export default function Addresses() { const { isAuthenticated } = useAuth(); const utils = trpc.useUtils(); const addresses = trpc.commerce.addresses.list.useQuery(undefined, { enabled: isAuthenticated }); const [form, setForm] = useState<AddressForm>(blank); const save = trpc.commerce.addresses.save.useMutation({ onSuccess: () => { utils.commerce.addresses.list.invalidate(); setForm(blank); toast.success("Address saved"); }, onError: e => toast.error(e.message) }); const remove = trpc.commerce.addresses.remove.useMutation({ onSuccess: () => { utils.commerce.addresses.list.invalidate(); toast.success("Address removed"); } }); if (!isAuthenticated) return <StorefrontLayout><div className="container py-20 text-center"><p className="font-serif text-3xl font-semibold">Sign in to manage addresses</p></div></StorefrontLayout>; return <StorefrontLayout><main className="container py-10 sm:py-14"><p className="eyebrow">Delivery details</p><h1 className="mt-2 font-serif text-5xl font-semibold">Your addresses</h1><div className="mt-8 grid gap-8 lg:grid-cols-[1fr_360px]"><section className="space-y-3">{addresses.isError && <div className="rounded-2xl border border-[#ae3f25]/30 bg-[#fff0e9] p-4 text-sm">We could not load your addresses. Please try again.</div>}{addresses.data?.map(address => <article key={address.id} className="rounded-2xl bg-white p-5"><div className="flex items-start justify-between gap-3"><div><p className="flex items-center gap-2 font-bold"><MapPin className="h-4 w-4 text-[#ae3f25]" />{address.label} {address.isDefault && <span className="rounded-full bg-[#fff0e9] px-2 py-0.5 text-[10px] text-[#ae3f25]">Default</span>}</p><p className="mt-3 text-sm leading-6 text-[#74695b]">{address.recipientName} · {address.phone}<br />{address.line1}{address.line2 ? `, ${address.line2}` : ""}<br />{address.city}, {address.state} {address.pincode}</p></div><div className="flex gap-2"><button onClick={() => setForm({ ...address, line2: address.line2 || "", deliveryInstructions: address.deliveryInstructions || "" })} className="rounded-lg p-2 text-[#74695b]"><Pencil className="h-4 w-4" /></button><button onClick={() => remove.mutate({ id: address.id })} className="rounded-lg p-2 text-[#ae3f25]"><Trash2 className="h-4 w-4" /></button></div></div></article>)}{addresses.data?.length === 0 && <div className="rounded-3xl bg-white p-8 text-sm text-[#74695b]">No saved addresses yet. Add one when you are ready to order.</div>}</section><aside className="h-fit rounded-3xl bg-[#272119] p-6 text-[#fff8ef]"><p className="text-xs font-bold uppercase tracking-[.2em] text-[#d99b69]">{form.id ? "Update address" : "New address"}</p><div className="mt-5 space-y-3">{Object.entries(form).filter(([key]) => !["id", "isDefault"].includes(key)).map(([key, value]) => <Input key={key} value={String(value)} onChange={event => setForm(current => ({ ...current, [key]: event.target.value }))} placeholder={key.replace(/([A-Z])/g, " $1")} className="border-white/15 bg-white/10 text-white placeholder:text-white/40" />)}<label className="flex items-center gap-2 text-xs"><input type="checkbox" checked={form.isDefault} onChange={event => setForm(current => ({ ...current, isDefault: event.target.checked }))} />Make default address</label><Button onClick={() => save.mutate({ ...form, line2: form.line2 || undefined, deliveryInstructions: form.deliveryInstructions || undefined })} disabled={save.isPending} className="w-full rounded-xl bg-[#ae3f25] hover:bg-[#8e301b]">Save address</Button>{form.id && <Button onClick={() => setForm(blank)} variant="outline" className="w-full border-white/20 bg-transparent text-white">Cancel edit</Button>}</div></aside></div></main></StorefrontLayout>; }

