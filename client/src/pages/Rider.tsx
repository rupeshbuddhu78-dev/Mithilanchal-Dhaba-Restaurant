import { useAuth } from "@/_core/hooks/useAuth";
import DashboardLayout, { type DashboardNavItem } from "@/components/DashboardLayout";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { CheckCircle2, Compass, LayoutDashboard, MapPinned, Navigation, PackageCheck } from "lucide-react";
import { toast } from "sonner";

const nav: DashboardNavItem[] = [{ icon: LayoutDashboard, label: "My deliveries", path: "/rider" }, { icon: MapPinned, label: "Location context", path: "/rider/location" }];
export default function Rider() { const { user, loading } = useAuth(); const allowed = user?.role === "rider"; const utils = trpc.useUtils(); const tasks = trpc.operations.rider.tasks.useQuery(undefined, { enabled: allowed }); const update = trpc.operations.rider.updateStatus.useMutation({ onSuccess: () => { utils.operations.rider.tasks.invalidate(); toast.success("Delivery progress updated"); }, onError: e => toast.error(e.message) }); const location = trpc.operations.rider.updateLocation.useMutation({ onSuccess: () => toast.success("Current location shared with operations"), onError: e => toast.error(e.message) }); const share = () => { if (!navigator.geolocation) return toast.error("Location is not available in this browser."); navigator.geolocation.getCurrentPosition(position => location.mutate({ latitude: position.coords.latitude, longitude: position.coords.longitude }), () => toast.error("Location permission was not granted."), { enableHighAccuracy: true, timeout: 10000 }); }; if (!loading && !allowed) return <div className="flex min-h-screen items-center justify-center bg-[#fbf8f2] p-6 text-center"><div><p className="font-serif text-3xl font-semibold">Rider access required</p><p className="mt-3 text-sm text-[#74695b]">Sign in with a rider account to see assigned deliveries.</p></div></div>; return <DashboardLayout menuItems={nav} brand="Delivery desk"><div className="mx-auto max-w-5xl py-5"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Delivery workspace</p><h1 className="mt-2 font-serif text-4xl font-semibold">Your deliveries, in context.</h1></div><Button onClick={share} disabled={location.isPending} className="rounded-xl bg-[#ae3f25] hover:bg-[#8e301b]"><Navigation className="mr-2 h-4 w-4" />Share my location</Button></div><div className="mt-7 grid gap-5 md:grid-cols-2">{tasks.data?.map(({ assignment, order }) => { const address = order.deliveryAddressSnapshot as { line1?: string; city?: string; pincode?: string }; const target = [address.line1, address.city, address.pincode].filter(Boolean).join(", "); const route = `https://www.openstreetmap.org/search?query=${encodeURIComponent(target)}`; return <article key={assignment.id} className="rounded-3xl bg-white p-6 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[.15em] text-[#ae3f25]">{order.orderNo}</p><h2 className="mt-2 font-serif text-2xl font-semibold">{order.status === "rider_assigned" ? "Ready for pickup" : "Out for delivery"}</h2></div><PackageCheck className="h-6 w-6 text-[#ae3f25]" /></div><div className="mt-6 rounded-2xl bg-[#fbf8f2] p-4"><p className="flex items-center gap-2 text-xs font-bold uppercase tracking-[.14em] text-[#74695b]"><MapPinned className="h-4 w-4" />Delivery location</p><p className="mt-2 text-sm leading-6">{target || "Delivery address available inside the order record."}</p><a href={route} target="_blank" rel="noreferrer" className="mt-3 inline-flex items-center gap-2 text-xs font-bold text-[#ae3f25]"><Compass className="h-3.5 w-3.5" />Open directions</a></div><div className="mt-5 flex gap-3">{order.status === "rider_assigned" && <Button onClick={() => update.mutate({ orderId: order.id, status: "out_for_delivery" })} disabled={update.isPending} className="flex-1 rounded-xl bg-[#272119]">Start delivery</Button>}{order.status === "out_for_delivery" && <Button onClick={() => update.mutate({ orderId: order.id, status: "delivered" })} disabled={update.isPending} className="flex-1 rounded-xl bg-[#658247] hover:bg-[#516938]"><CheckCircle2 className="mr-2 h-4 w-4" />Mark delivered</Button>}</div></article>; })}{tasks.data?.length === 0 && <div className="rounded-3xl bg-white p-10 text-center md:col-span-2"><PackageCheck className="mx-auto h-8 w-8 text-[#ae3f25]" /><p className="mt-4 font-serif text-2xl font-semibold">No deliveries assigned right now</p><p className="mt-2 text-sm text-[#74695b]">Your next delivery will appear here once restaurant staff assigns it.</p></div>}</div></div></DashboardLayout>; }

